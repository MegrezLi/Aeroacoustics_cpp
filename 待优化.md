# 待优化

本文件记录 Aeroacoustics_cpp 的后续审查事项，供选择实施范围。共 24 项：可靠性 3 项、计算效率 7 项、结构扩展 5 项、工程能力与验证 9 项。编号沿用前次清单；E9 为前次单独说明的 TNO 外缘速度比问题。

源码基线：[`3c24ded`](https://github.com/MegrezLi/Aeroacoustics_cpp/commit/3c24dedfc4282fff88d0aa533480e7dd118e98de)。R1–R3 已于 2026-09-11 完成，其余事项仍待选择。下方代码块保留审查时的原始快照，用于说明修改前的问题；当前实现及验证见 [R1–R3 可靠性说明](docs/reliability.md)。现有运行仍是独立 C++；Python 片段只来自测试调度脚本。

代码块直接摘录对应提交的源码，保留原语句和已有注释，仅插入以 `// [审查]` 或 `# [审查]` 开头的说明。每块标出原文件及原始行号范围；部分代码块是函数片段，不是完整可编译函数。删除新增的审查注释后，各片段与原文件逐行一致。工程能力尚未实现的项目，引用的是当前限制、数据结构或未来连接位置，不虚构缺失模块的源码。

性能项是静态审查得到的候选，尚未单独量化收益。实施前应先记录模块耗时、内存分配、Newton/BEM 迭代数及输出开销。已有两个整机工况和普通/MKL 声学公式对照继续作为回归基线；不能把 Fortran 对照精度等同于实测预测精度。此前优化数据见 [优化记录](docs/optimization.md)。

## 事项索引

| 编号 | 事项 | 优先级 | 状态 |
| --- | --- | --- | --- |
| [R1](#r1) | 非有限声级被统一转换为零声能 | 高 | 已完成 |
| [R2](#r2) | 零声能语义和文件写入检查 | 高 | 已完成 |
| [R3](#r3) | 查表超出有效范围时缺少诊断 | 高 | 已完成 |
| [P1](#p1) | 共享同一状态下的结构运动学 | 高 | 待选择 |
| [P2](#p2) | 复用气动、运动映射和载荷映射缓冲区 | 高 | 待选择 |
| [P3](#p3) | 只在声学采样时执行必要的边界层插值 | 高（表格边界层工况） | 待选择 |
| [P4](#p4) | 预计算 TNO 边界层剖面并改善内存遍历 | 中（TNO 工况） | 待选择 |
| [P5](#p5) | 按输出需求聚合并分块处理观察点 | 中；大网格时高 | 待选择 |
| [P6](#p6) | 在独立工况、节点或观察点层面并行 | 按规模选择 | 待选择 |
| [P7](#p7) | 结构 Jacobian 和收敛诊断 | 后置 | 待选择 |
| [S1](#s1) | 拆分整机入口的装配、调度、适配和输出 | 高 | 待选择 |
| [S2](#s2) | 集中管理噪声机制、模型选择和通道信息 | 高 | 待选择 |
| [S3](#s3) | 明确所有权、生命周期和状态修改边界 | 高（作为库使用时） | 待选择 |
| [S4](#s4) | 明确频带、单位、计权和声学量的类型 | 高 | 待选择 |
| [S5](#s5) | 解耦固定自由度与整机模块组合 | 按新增物理需求实施 | 待选择 |
| [E1](#e1) | 增加真实入流、变速变桨和降噪运行模式 | 高（实机设计） | 待选择 |
| [E2](#e2) | 建立独立的室外声传播模块 | 高（场址/居民点评价） | 待选择 |
| [E3](#e3) | 完善真实翼型、转捩和叶片表面状态输入 | 高（实机设计） | 待选择 |
| [E4](#e4) | 增加候选尾缘模型和锯齿等降噪装置 | 按研究目标实施 | 待选择 |
| [E5](#e5) | 时间统计、幅度调制、运动声源和音调 | 高（声音特征评价） | 待选择 |
| [E6](#e6) | 机械噪声、塔架效应、多机尾流和风场噪声 | 按机型/场址需求实施 | 待选择 |
| [E7](#e7) | 提供与工程测量对应的统计输出 | 高（工程交付） | 待选择 |
| [E8](#e8) | 建立独立实测验证和不确定性评估 | 高 | 待选择 |
| [E9](#e9) | 明确 TNO 外缘速度比的参考兼容约定 | 与 E3/E4 联动 | 待选择 |

## 可靠性

<a id="r1"></a>

### R1　非有限声级被统一转换为零声能

**优先级：高；性质：可靠性风险；状态：已完成（2026-09-11）。**

负无穷可以表示关闭的声源；NaN 和正无穷可能表示计算异常。当前汇总没有区分这三种情况。这是异常处理路径的风险，不表示已有两个验证工况出现过此问题。

源码：[src/apps/turbine_main.cpp，第 139–147 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L139-L147)。

```cpp
                        for (std::size_t m = 0; m < 7; ++m)
                            for (std::size_t f = 0; f < nf; ++f) {
                                const double db = (*snapshot)[o][n][m][f],
                                             // [审查] R1：isfinite 对 -Inf、+Inf 和 NaN 都返回 false，全部进入零声能分支。
                                             // [审查] 优化方向：仅将约定的静音状态视为零；NaN/+Inf 应报告模型、时间、叶片、节点和频率。
                                             // [审查] 原因：把计算失败当成静音，可能掩盖异常并低估总声级。
                                             power = std::isfinite(db) ? std::pow(10., db / 10) : 0;
                                total[o] += power;
                                spectra[o * nf + f] += power;
                                mechanisms[(o * nf + f) * 7 + m] += power;
                                node_power += power;
                            }
```

已覆盖的验证重点：分别检查有限声级、关闭声源、NaN 和正无穷；合法输入结果应保持一致，异常应能准确定位。

<a id="r2"></a>

### R2　零声能语义和文件写入检查

**优先级：高；性质：可靠性风险；状态：已完成（2026-09-11）。**

需要分别处理“没有声能”“没有该输出”“计算无效”和物理上的 0 dB。声学文件已有打开检查，但所有输出仍需要写入失败检查，动力学和元数据文件还缺少打开失败检查。

源码：[src/apps/turbine_main.cpp，第 9–11 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L9-L11)。

```cpp
namespace {
// [审查] R2：这里将零声能写成 0 dB；0 dB 本身是有效物理声级，不等于没有声音。
// [审查] 建议：内部保留静音/无效状态；旧格式可继续兼容，但应提供掩码或明确的输出说明。
double decibels(double p) { return p == 0 ? 0 : 10 * std::log10(p); }
} // namespace
```

源码：[src/apps/turbine_main.cpp，第 96–101 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L96-L101)。

```cpp
        // [审查] R2：创建 dynamics 后未立即检查流状态；路径权限或磁盘问题可能导致输出失败。
        std::ofstream dynamics(directory / "dynamics.csv");
        dynamics << std::setprecision(17) << "time";
        for (int b = 1; b <= 3; ++b)
            dynamics << ",q" << b << "_flap1,q" << b << "_flap2,q" << b << "_edge,qd" << b << "_flap1,qd" << b
                     << "_flap2,qd" << b << "_edge";
        dynamics << '\n';
```

源码：[src/apps/turbine_main.cpp，第 163–170 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L163-L170)。

```cpp
        // [审查] R2：metadata 也未检查打开状态。建议统一 OutputWriter，检查打开、写入、flush/close 结果。
        std::ofstream metadata(directory / "run.json");
        metadata << std::setprecision(17) << "{\n  \"solver\": \"standalone C++\",\n  \"dt\": " << c.dt
                 << ",\n  \"duration\": " << solver.time << ",\n  \"steps\": " << solver.step_number
                 << ",\n  \"acoustic_samples\": " << snapshots << ",\n  \"elapsed_seconds\": "
                 << std::chrono::duration<double>(std::chrono::steady_clock::now() - started).count()
                 << "\n}\n";
        // [审查] R2：应在确认各输出成功后报告完成，避免返回成功但文件缺失或截断。
        std::cout << "C++ turbine completed " << solver.time << " s, " << solver.step_number << " steps, "
                  << snapshots << " acoustic samples\n";
```

已覆盖的验证重点：零值与关闭通道语义、不可写路径及写入中断；保留旧输出格式时明确兼容规则。

<a id="r3"></a>

### R3　查表超出有效范围时缺少诊断

**优先级：高；性质：工程可靠性；状态：已完成（2026-09-11）。**

翼型系数和边界层表越界后采用端点值。端点保持可作为明确的策略，但不应让用户无法知道多少节点、多少时刻已经离开数据有效范围。

源码：[src/turbine/io/case_input.cpp，第 184–194 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/io/case_input.cpp#L184-L194)。

```cpp
Coefficients Airfoil::at(double a) const {
    a = std::remainder(a, 2 * pi);
    // [审查] R3：攻角低于表格范围后使用首行，高于范围则使用末行。
    // [审查] 建议累计越界位置和持续时间，允许选择报错、警告或经验证的外推。
    if (a <= alpha.front())
        return coefficients.front();
    if (a >= alpha.back())
        return coefficients.back();
    const auto j = static_cast<std::size_t>(std::upper_bound(alpha.begin(), alpha.end(), a) - alpha.begin());
    const double w = (a - alpha[j - 1]) / (alpha[j] - alpha[j - 1]);
    const auto &l = coefficients[j - 1];
    const auto &r = coefficients[j];
    return {l.cl + w * (r.cl - l.cl), l.cd + w * (r.cd - l.cd), l.cm + w * (r.cm - l.cm)};
```

源码：[src/acoustics/driver.cpp，第 36–42 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/driver.cpp#L36-L42)。

```cpp
std::tuple<std::size_t, std::size_t, double> bounds(const Spectrum &grid, double x) {
    if (grid.size() == 1)
        return {0, 0, 0};
    auto pos = std::upper_bound(grid.begin(), grid.end(), x) - grid.begin();
    std::size_t a = std::clamp<std::ptrdiff_t>(pos - 1, 0, grid.size() - 2);
    // [审查] R3：插值权重被限制到 [0,1]，超界值退化为端点值。
    // [审查] 原因：改变风速、桨距或翼型后，静默使用端点可能让结果超出模型适用范围。
    return {a, a + 1, std::clamp((x - grid[a]) / (grid[a + 1] - grid[a]), 0., 1.)};
}
```

已覆盖的验证重点：表内、端点、表外的攻角与雷诺数；既检查数值，也检查超界报告是否完整。


## 计算效率

<a id="p1"></a>

### P1　共享同一状态下的结构运动学

**优先级：高；性质：性能候选；状态：待选择。**

同一时间和叶片的基底被多个节点重复计算；载荷映射和结构加速度计算又分别查询同一状态下的节点运动。可按时间、叶片和迭代状态共享运动学结果。

源码：[src/turbine/structure/blade_dynamics.cpp，第 142–157 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/structure/blade_dynamics.cpp#L142-L157)。

```cpp
// [审查] P1：基底中的方位角和叶片姿态与节点编号无关，可在给定时间、叶片下准备一次。
Matrix3 BladeStructure::blade_basis(double time, std::size_t blade) const {
    const Vec3 c2{-std::sin(tilt_) * std::cos(yaw_), -std::sin(tilt_) * std::sin(yaw_), std::cos(tilt_)},
        c3{std::sin(yaw_), -std::cos(yaw_), 0};
    const double a = initial_azimuth + omega * time + 2 * pi * blade / 3;
    const Vec3 g2 = std::cos(a) * c2 + std::sin(a) * c3, g3 = -std::sin(a) * c2 + std::cos(a) * c3;
    const Vec3 i1 = std::cos(cone[blade]) * shaft - std::sin(cone[blade]) * g3,
               i3 = std::sin(cone[blade]) * shaft + std::cos(cone[blade]) * g3;
    return {{std::cos(pitch[blade]) * i1 - std::sin(pitch[blade]) * g2,
             std::sin(pitch[blade]) * i1 + std::cos(pitch[blade]) * g2, i3}};
}
Motion BladeStructure::motion(double time, std::size_t blade, const ModalState &state,
                              const StructuralStation &s) const {
    // [审查] P1：每次 motion 都重新调用 blade_basis，包含重复三角函数计算。
    // [审查] 建议传入预计算基底，或由 BladeKinematics 工作区按状态统一生成节点运动。
    const auto b = blade_basis(time, blade);
    const auto &q = state.q;
    const auto &qd = state.qd;
    Motion y;
```

源码：[src/turbine/structure/blade_dynamics.cpp，第 181–191 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/structure/blade_dynamics.cpp#L181-L191)。

```cpp
Vec3 BladeStructure::acceleration(double time, std::size_t blade, const ModalState &s,
                                  const std::vector<PointLoad> &loads) const {
    if (loads.size() != nodes.size())
        throw std::runtime_error("Structural load count mismatch");
    Matrix3 mass{};
    Vec3 force{};
    for (std::size_t j = 1; j < nodes.size(); ++j) {
        // [审查] P1：载荷映射中已查询过同一叶片状态的部分节点位置，此处又求完整运动。
        // [审查] 可共享节点运动，但 Newton 扰动或状态更新后必须重新计算，不能误用旧缓存。
        const auto motion_j = motion(time, blade, s, nodes[j]);
        const double m = j + 1 == nodes.size() ? tip_mass[blade] : nodes[j].mass;
        const Vec3 f = loads[j].force - m * (Vec3{0, 0, gravity_} + motion_j.acceleration_bias);
        for (int i = 0; i < 3; ++i) {
```

后续验证重点：记录 motion/basis 调用次数和耗时；复核映射、结构响应及两个整机工况，防止扰动缓存失效。

<a id="p2"></a>

### P2　复用气动、运动映射和载荷映射缓冲区

**优先级：高；性质：性能候选；状态：待选择。**

现有声学工作区已经复用数组，整机气动和映射路径仍有每步创建向量的地方。优先减少重复分配，不把正常的返回值优化误判为必然深复制。

源码：[src/turbine/coupling/rotor.cpp，第 51–61 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/coupling/rotor.cpp#L51-L61)。

```cpp
RotorOutput Rotor::evaluate(double time, const RotorState &state) const {
    const auto &c = *case_;
    // [审查] P2：每次 evaluate 创建新的 RotorOutput，其中叶片向量随后重新分配。
    RotorOutput y;
    const std::size_t count = c.stations.size();
    for (int b = 0; b < 3; ++b) {
        // [审查] P2：structural 每片叶片重新建立，push_back 还可能扩容。
        // [审查] 建议由调用者或 RotorWorkspace 持有缓冲区，提供 evaluate_into/transfer_into 一类填充接口。
        std::vector<Motion> structural;
        for (const auto &node : structure.nodes)
            structural.push_back(structure.motion(time, b, state[b], node));
        const auto aerodynamic = motion_maps_[b].transfer(structural);
        y.blades[b].resize(count);
        // Removing prescribed pitch from the blade basis is equivalent to the
```

源码：[src/turbine/coupling/mesh.cpp，第 24–29 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/coupling/mesh.cpp#L24-L29)。

```cpp
}
std::vector<Motion> MotionMap::transfer(const std::vector<Motion> &source) const {
    if (source.size() != source_.size())
        throw std::runtime_error("Motion source count mismatch");
    // [审查] P2：MotionMap 每次构造结果向量，可使用预分配目标数组。
    std::vector<Motion> result(destination_.size());
    for (std::size_t i = 0; i < result.size(); ++i) {
```

源码：[src/turbine/coupling/mesh.cpp，第 92–98 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/coupling/mesh.cpp#L92-L98)。

```cpp
std::vector<PointLoad> LoadMap::transfer(const std::vector<PointLoad> &loads, const std::vector<Vec3> &source,
                                         const std::vector<Vec3> &destination) const {
    if (loads.size() != source_count_ || source.size() != source_count_ ||
        destination.size() != destination_count_)
        throw std::runtime_error("Load mesh count mismatch");
    // [审查] P2：LoadMap 也分配结果向量；复用时必须清零，防止上一轮载荷残留。
    std::vector<PointLoad> result(destination_count_);
    for (const auto &s : segments_) {
```

后续验证重点：统计稳态时间循环的分配次数；与旧输出对照，特别检查数组清零、大小变化和多实例独立性。

<a id="p3"></a>

### P3　只在声学采样时执行必要的边界层插值

**优先级：高（表格边界层工况）；性质：性能候选；状态：待选择。**

主循环每个动力学时间步都对所有节点执行边界层插值，之后才判断声学是否采样。默认 DT_AA/DT=16；这部分可按声学采样调度，但速度与 TI 历史状态仍须按原时间顺序更新。默认经验边界层分支不执行这些插值，因此该项不能算作默认工况的既定收益。

源码：[src/apps/turbine_main.cpp，第 127–132 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L127-L132)。

```cpp
                    // [审查] P3：启用表格边界层时，每步都进入插值；即使该步不输出声学、节点未参与发声也会计算。
                    if (tables[j])
                        node.section.bl = tables[j]->interpolate(
                            node.section.alpha_deg, a.speed * node.section.chord / c.nu, node.section.chord);
                }
            // [审查] P3：采样判断在插值之后。建议统一声学采样计划，将纯声学所需的插值延迟到采样时。
            const auto *snapshot = acoustic.step_view(solver.time, nodes);
            if (snapshot) {
```

源码：[src/acoustics/driver.cpp，第 79–85 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/driver.cpp#L79-L85)。

```cpp
BoundaryLayer BLTable::interpolate(double alpha, double re, double chord) const {
    // [审查] P3：每次插值完整扫描 AoA 轴和 Re 轴。表格冻结后可在加载时校验一次。
    // [审查] 前提：BLTable 的数组目前可公开修改，必须先约束可变性，不能直接删除运行时检查。
    increasing(aoa);
    increasing(reynolds);
    require(values.size() == aoa.size() * reynolds.size(), "Invalid BL shape");
    require(std::isfinite(alpha) && std::isfinite(re) && chord > 0, "Invalid BL interpolation input");
    auto [a, b, t] = bounds(aoa, alpha);
    auto [c, d, s] = bounds(reynolds, re);
```

后续验证重点：比较采样数、选中节点、TI 更新时序及 BLMod=2/TNO 结果；测量插值调用次数。

<a id="p4"></a>

### P4　预计算 TNO 边界层剖面并改善内存遍历

**优先级：中（TNO 工况）；性质：性能候选；状态：待选择。**

TNO 每个频率调用积分时，会重复计算该侧相同的边界层剖面。当前还按 j 外层、i 内层写入 i*61+j 布局，写入步长为 61。可先缓存剖面，再使主循环按连续内存遍历；不能简单交换循环后反而把剖面计算重复 61 次。

源码：[src/acoustics/tno.cpp，第 53–73 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/tno.cpp#L53-L73)。

```cpp
    const double alpha = suction ? .45 : .30, kappa = .41, cmu = .09, cnuk = 5.5;
    // [审查] P4：ustar、wf 以及下方随高度的 length/u/grad/ke 等，在同一节点、同一侧对频率不变。
    // [审查] 建议按侧预计算 61 点剖面，供该采样时刻所有频率使用。
    const double ustar = uo * std::sqrt(cf / 2.);
    const double wf = uo / ustar - std::log(ustar * delta / p.kinvisc) / kappa - cnuk;
    for (int j = 0; j < 61; ++j) {
        const double x = x2[j], length = .085 * delta * std::tanh(kappa * x / (.085 * delta));
        const double u = ustar * (std::log(ustar * x / p.kinvisc) / kappa + cnuk +
                                  wf * .5 * (1 - std::cos(pi * x / delta)));
        const double grad = ustar * (1 / (kappa * x) + wf * .5 * pi / delta * std::sin(pi * x / delta));
        const double ke = std::sqrt(pi) / length * .4213560764;
        const double nut = std::pow(length * kappa, 2) * std::abs(grad);
        const double ums = alpha * std::sqrt(std::pow(nut * grad, 2) / cmu), uc = .7 * u,
                     ag = .05 * uc / length;
        for (int i = 0; i < 61; ++i) {
            // [审查] P4：内层 i 递增时地址跨 61 个 double；可以在剖面缓存后按行连续装配。
            // [审查] 原因：改善缓存局部性和向量化机会；实际收益需要计时，不改变积分权重与求和含义。
            const int idx = i * 61 + j;
            const double kh = wave[i] / ke;
            const double phi22 = 4. / 9. / pi / (ke * ke) * (kh * kh) / std::pow(1 + kh * kh, 7. / 3.);
            factor[idx] = length * ums * grad * grad * phi22 / (ag * std::sqrt(pi));
            gauss[idx] = -std::pow((omega - uc * wave[i]) / ag, 2);
            decay[idx] = -2 * std::abs(wave[i]) * x;
        }
    }
```

源码：[src/acoustics/tno.cpp，第 97–107 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/tno.cpp#L97-L107)。

```cpp
    const double ratio = std::pow(2., 1. / 3.);
    out.bandwidth.resize(p.freqlist.size());
    for (int side = 0; side < 2; ++side) {
        out.active[side] = s.bl.cf[side] > 0;
        out.integral[side].resize(p.freqlist.size());
        for (std::size_t i = 0; i < p.freqlist.size(); ++i) {
            const double omega = 2 * pi * p.freqlist[i];
            // [审查] P4：带宽只与频率定义有关，却在两侧和每个节点重复计算，可放入不可变频带配置。
            out.bandwidth[i] = 2 * omega * (std::sqrt(ratio) - 1 / std::sqrt(ratio));
            out.integral[side][i] = out.active[side] ? integrate_tno(omega, 0, 10 * omega / s.speed,
                                                                     side == 0, out.mach, s.bl, p, work)
                                                     : 0.;
```

后续验证重点：普通/MKL 两个后端的 TNO Fortran 对照、极端边界层输入及不同节点/观察点规模的计时。

<a id="p5"></a>

### P5　按输出需求聚合并分块处理观察点

**优先级：中；大网格时高；性质：性能与内存候选；状态：待选择。**

只输出总声级时仍生成完整快照并累加四类结果。大观察点网格下可分块计算并聚合，仅保存用户请求的量。声源机制可以不保存分项，但不能因为不输出分项而遗漏它对总噪声的贡献。

源码：[src/apps/turbine_main.cpp，第 104–108 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L104-L108)。

```cpp
        const std::size_t nf = parameters.freqlist.size(), no = observers.size();
        // [审查] P5：无论 NrOutFile 是多少，四类数组均分配。建议根据输出请求建立聚合缓冲区。
        std::array<std::vector<double>, 4> values{{std::vector<double>(no), std::vector<double>(no * nf),
                                                   std::vector<double>(no * nf * 7),
                                                   std::vector<double>(3 * c.stations.size() * no)}};
        auto &total = values[0], &spectra = values[1], &mechanisms = values[2], &nodal = values[3];
```

源码：[src/acoustics/workspace.cpp，第 40–50 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/workspace.cpp#L40-L50)。

```cpp
    // [审查] P5：为全部观察点保留节点×机制×频率快照；受声点数量增加后内存随之线性增长。
    // [审查] 可增加分块观察点/回调聚合接口；仍保留现有完整快照接口供分析使用。
    work.output.resize(observers.size());
    for (std::size_t o = 0; o < observers.size(); ++o) {
        auto &output = work.output[o];
        output.resize(nodes.size());
        for (std::size_t n = 0; n < nodes.size(); ++n) {
            const auto &node = nodes[n];
            const auto geometry = observe(observers[o], node.aero_center, node.global_to_local,
                                          node.section.chord, node.airfoil_reference);
            detail::emit_section(work.parameters, work.sources[n], geometry.first, geometry.second,
                                 work.weighting, output[n]);
        }
```

后续验证重点：总量与分项相互核对；观察点分块前后结果一致，报告内存峰值和运行时间。

<a id="p6"></a>

### P6　在独立工况、节点或观察点层面并行

**优先级：按规模选择；性质：性能候选；状态：待选择。**

优先并行独立工况，再评估单次声学计算的并行。三片叶片或少量观察点的任务很小，不保证线程化有效。当前工作区不能被多个线程无保护地同时写入。

源码：[src/acoustics/workspace.cpp，第 5–10 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/workspace.cpp#L5-L10)。

```cpp
struct AcousticWorkspace::Impl {
    Parameters parameters;
    Spectrum weighting;
    // [审查] P6：当前所有节点共用一个可变 TnoWorkspace；直接并行下方节点循环会发生竞争。
    detail::TnoWorkspace integration;
    std::vector<detail::PreparedSection> sources;
    Snapshot output;
```

源码：[src/acoustics/workspace.cpp，第 35–49 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/workspace.cpp#L35-L49)。

```cpp
    work.sources.resize(nodes.size());
    // Boundary layers and all source spectral shapes depend on the section,
    // not on the observer. Prepare them exactly once for this snapshot.
    // [审查] P6：不同节点声源准备可并行，但每个工作线程必须有独立积分临时数组。
    for (std::size_t n = 0; n < nodes.size(); ++n)
        detail::prepare_section(work.parameters, nodes[n].section, work.integration, work.sources[n]);
    work.output.resize(observers.size());
    // [审查] P6：声源准备完成后，观察点也有并行机会；输出容器应提前定尺寸并划分写入范围。
    // [审查] 同时控制 MKL 内部线程，防止外层线程数与库线程数相乘造成过度竞争。
    for (std::size_t o = 0; o < observers.size(); ++o) {
        auto &output = work.output[o];
        output.resize(nodes.size());
        for (std::size_t n = 0; n < nodes.size(); ++n) {
            const auto &node = nodes[n];
            const auto geometry = observe(observers[o], node.aero_center, node.global_to_local,
                                          node.section.chord, node.airfoil_reference);
            detail::emit_section(work.parameters, work.sources[n], geometry.first, geometry.second,
                                 work.weighting, output[n]);
```

后续验证重点：单线程/多线程输出、线程检测和不同任务规模；不要跨时间步任意并行依赖历史状态的求解。

<a id="p7"></a>

### P7　结构 Jacobian 和收敛诊断

**优先级：后置；性质：数值方法调整；状态：待选择。**

当前有限差分 Jacobian 每轮更新，扰动量、迭代上限和修正阈值固定。应先记录残差、尺度、迭代数与失败位置，再评估尺度相关扰动、Jacobian 复用、解析导数或自动微分。它会改变数值路径，风险高于缓存优化。

源码：[src/turbine/coupling/solver.cpp，第 33–49 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/coupling/solver.cpp#L33-L49)。

```cpp
    // [审查] P7：12 次上限固定。可集中为 SolverOptions，并记录每步实际迭代数。
    for (int iteration = 0; iteration < 12; ++iteration) {
        const auto loads = rotor.structural_loads(next_time, state, aerodynamic);
        double largest = 0;
        for (int b = 0; b < 3; ++b) {
            const auto f = rotor.structure.acceleration(next_time, b, state[b], loads[b]);
            const Vec3 residual = f - current_acc[b];
            Matrix3 jac{};
            for (int j = 0; j < 3; ++j) {
                // [审查] P7：固定加速度扰动 h；换机型或尺度后不一定仍是合适选择。
                const double h = 1e-4;
                auto perturbed = state[b];
                perturbed.q[j] += beta_prime_ * h;
                perturbed.qd[j] += gamma_prime_ * h;
                const auto lp = rotor.structural_loads_for_blade(b, next_time, perturbed, aerodynamic);
                const auto fp = rotor.structure.acceleration(next_time, b, perturbed, lp);
                // [审查] P7：这里构造差分导数。若复用 Jacobian，需要残差恶化时重建，不能无条件跨步复用。
                for (int i = 0; i < 3; ++i)
                    jac[i][j] = (i == j ? 1. : 0.) - (fp[i] - f[i]) / h;
            }
```

源码：[src/turbine/coupling/solver.cpp，第 56–60 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/coupling/solver.cpp#L56-L60)。

```cpp
        // [审查] P7：当前仅使用修正量判停；建议同时检查有尺度的残差和有限值。
        // [审查] 原因：扩展工况下，小修正量不必然意味着物理方程残差已足够小。
        if (largest < 1e-9)
            break;
        if (iteration == 11)
            throw std::runtime_error("Structural solve failed at time " + std::to_string(next_time));
    }
```

后续验证重点：跨时间步长、初值和工况测试残差与收敛；保存参考求解模式，不把 3×3 求解机械替换为 MKL。


## 函数、接口与模块扩展

<a id="s1"></a>

### S1　拆分整机入口的装配、调度、适配和输出

**优先级：高；性质：结构扩展；状态：待选择。**

main 同时负责输入解析、声学模型装配、时间循环、气动数据转换和输出。建议建立仿真驱动、声学输入适配器、结果聚合器和输出器，让同一套 C++ 求解能力可被命令行、批处理和设计优化调用。

源码：[src/apps/turbine_main.cpp，第 24–40 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L24-L40)。

```cpp
        // [审查] S1：main 直接解析声学参数并覆盖环境值，还组织节点和模型。可交给统一 CaseBuilder/初始化层。
        auto parameters = aeroacoustics::read_aa_input(c.acoustic.path.string()).first;
        parameters.airdens = c.rho;
        parameters.kinvisc = c.nu;
        parameters.spdsound = c.sound_speed;
        const auto observers = aeroacoustics::read_observers(c.acoustic.file("ObserverLocations").string());
        aeroacoustics::Spectrum span;
        for (const auto &s : c.stations)
            span.push_back(s.span);
        const auto elements = aeroacoustics::blade_elements(span, c.acoustic.number("BldPrcnt"));
        const auto first = elements.first;
        const int output_count = c.acoustic.integer("NrOutFile");
        // [审查] S1：声学驱动的构造参数较多，应由具名配置对象传递，减少位置参数误用。
        aeroacoustics::AcousticDriver acoustic(
            parameters, span, 3, observers, c.acoustic.number("DT_AA", c.dt), c.acoustic.number("AAStart"),
            c.acoustic.number("BldPrcnt"), c.structure.number("TowerHt") + c.structure.number("Twr2Shft"),
            c.acoustic.integer("TICalcMeth"));
        std::vector<std::vector<aeroacoustics::Node>> nodes(3);
        std::vector<std::optional<aeroacoustics::BLTable>> tables(c.stations.size());
```

源码：[src/apps/turbine_main.cpp，第 116–131 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L116-L131)。

```cpp
            for (int b = 0; b < 3; ++b)
                for (std::size_t j = 0; j < c.stations.size(); ++j) {
                    // [审查] S1：AeroStation→Node 的坐标、角度和边界层转换嵌在主循环，应提取为独立适配器。
                    // [审查] 原因：增加另一种气动求解器或导入节点数据时，可复用声学驱动和输出层。
                    const auto &a = solver.aerodynamic.blades[b][j];
                    auto &node = nodes[b][j];
                    node.aero_center = a.motion.position;
                    node.inflow = a.wind;
                    node.section.speed = a.speed;
                    node.section.alpha_deg = a.alpha / turbine::deg;
                    for (int i = 0; i < 3; ++i)
                        for (int k = 0; k < 3; ++k)
                            node.global_to_local[3 * i + k] = a.motion.orientation[i][k];
                    if (tables[j])
                        node.section.bl = tables[j]->interpolate(
                            node.section.alpha_deg, a.speed * node.section.chord / c.nu, node.section.chord);
                }
            const auto *snapshot = acoustic.step_view(solver.time, nodes);
```

后续验证重点：拆分前后完整输出相同；用库接口运行算例，不依赖命令行入口中的局部状态。

<a id="s2"></a>

### S2　集中管理噪声机制、模型选择和通道信息

**优先级：高；性质：结构扩展；状态：待选择。**

七个物理机制可以继续作为稳定的标准输出；需要把机制与具体模型选择分开。更换某机制的模型，不一定增加新通道；真正增加机械噪声等机制时，才需要扩展通道描述。

源码：[include/aeroacoustics.hpp，第 12–15 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/include/aeroacoustics.hpp#L12-L15)。

```cpp
namespace aeroacoustics {
using Spectrum = std::vector<double>;
// [审查] S2：机制个数和数组下标固化。建议引入 Mechanism 枚举及集中管理的名称、单位、贡献规则。
using Mechanisms = std::array<Spectrum, 7>;
using Vec3 = std::array<double, 3>;
```

源码：[src/acoustics/spectrum.cpp，第 114–123 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/spectrum.cpp#L114-L123)。

```cpp
    if (p.lammod && p.itrip == 0)
        emit_laminar(s.laminar, trailing, out[0]);
    // [审查] S2：当前先准备 BPM 尾缘分量，TNO 再覆盖压力面和吸力面，分离分量仍来自 BPM。
    if (p.tbltemod)
        emit_trailing_edge(s.trailing_edge, trailing, out[1], out[2], out[3]);
    // [审查] S2：新增模型时必须表达“替代”关系，不能把多个完整尾缘模型简单相加导致重复计数。
    // [审查] 建议初始化时选择实现，保留小型数学函数，不必给每条公式建立虚函数体系。
    if (p.tbltemod == 2)
        emit_tno(s.tno, trailing, out[1], out[2]);
    if (p.bluntmod)
        emit_blunt(s.blunt, trailing, out[4]);
    if (p.tipmod && s.section.is_tip)
        emit_tip(s.tip, trailing, out[5]);
```

后续验证重点：机制开关组合、替代关系、通道顺序和总量；保留旧 C ABI/输出格式的映射。

<a id="s3"></a>

### S3　明确所有权、生命周期和状态修改边界

**优先级：高（作为库使用时）；性质：接口可靠性；状态：待选择。**

当前 main 中 Case 比 Solver 活得更久，已有调用方式是安全的；但类接口没有强制这一关系。外部创建临时 Case、销毁数据或移动/修改翼型容器后，非拥有指针可能失效。

源码：[include/turbine/rotor.hpp，第 32–38 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/include/turbine/rotor.hpp#L32-L38)。

```cpp
  private:
    // [审查] S3：Rotor 不拥有 Case，却在后续求解继续访问它。应明确生命周期契约或持有共享的不可变模型数据。
    const Case *case_;
    BEMOptions options_;
    struct SkewOptions {
        bool redistribute;
        double factor;
    } skew_;
```

源码：[include/turbine/unsteady.hpp，第 13–16 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/include/turbine/unsteady.hpp#L13-L16)。

```cpp
  private:
    // [审查] S3：指针依赖外部 Airfoil 地址稳定。模型数据发布后应冻结，避免容器扩容/移动使地址失效。
    const Airfoil *af_;
    double chord_, dt_, sound_;
    bool enabled_, first_ = true;
```

源码：[include/turbine/solver.hpp，第 4–13 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/include/turbine/solver.hpp#L4-L13)。

```cpp
class Solver {
  public:
    explicit Solver(const Case &);
    // [审查] S3：模型和状态大多公开可写，外部可改变 state/time 却不更新 aerodynamic 或历史状态。
    // [审查] 建议提供只读访问、受控初始化和状态恢复接口，保证耦合状态一致。
    Rotor rotor;
    RotorState state{};
    RotorOutput aerodynamic;
    std::array<Vec3, 3> acceleration{}, algorithmic{};
    double time = 0;
    std::size_t step_number = 0;
    void step();
```

后续验证重点：多求解实例、构造/销毁顺序、复制/移动语义以及重启后的历史状态一致性。

<a id="s4"></a>

### S4　明确频带、单位、计权和声学量的类型

**优先级：高；性质：数值语义与扩展；状态：待选择。**

现有频率检查仅要求递增；TNO 按固定 1/3 倍频程关系换算带宽。任意密集频率不能直接当作互不重叠的频带求和。PSD、频带均方声压、声级、A 计权及声功率应有明确语义。

源码：[src/acoustics/spectrum.cpp，第 15–23 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/spectrum.cpp#L15-L23)。

```cpp
void validate(const Parameters &p) {
    if (p.freqlist.empty())
        throw std::invalid_argument("Empty frequency list");
    double last = 0;
    // [审查] S4：检查频率合法性，但没有定义频带上下限或是否允许重叠。
    for (double f : p.freqlist) {
        if (!std::isfinite(f) || f <= last)
            throw std::invalid_argument("Frequencies must be finite, positive and increasing");
        last = f;
    }
```

源码：[src/acoustics/tno.cpp，第 97–107 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/tno.cpp#L97-L107)。

```cpp
    // [审查] S4：这里固定采用 1/3 倍频程比。更换频率列表并不会自动成为窄带模型。
    const double ratio = std::pow(2., 1. / 3.);
    out.bandwidth.resize(p.freqlist.size());
    for (int side = 0; side < 2; ++side) {
        out.active[side] = s.bl.cf[side] > 0;
        out.integral[side].resize(p.freqlist.size());
        for (std::size_t i = 0; i < p.freqlist.size(); ++i) {
            const double omega = 2 * pi * p.freqlist[i];
            // [审查] S4：建议 FrequencyBands 显式保存带宽与单位，PSD 经频带积分后再求声级。
            // [审查] 原因：重叠频带直接相加或将 dB/Hz 当 dB，会使总声级含义错误。
            out.bandwidth[i] = 2 * omega * (std::sqrt(ratio) - 1 / std::sqrt(ratio));
            out.integral[side][i] = out.active[side] ? integrate_tno(omega, 0, 10 * omega / s.speed,
                                                                     side == 0, out.mach, s.bl, p, work)
                                                     : 0.;
```

后续验证重点：用已知 PSD 的频带积分、倍频程合并和 A 计权核对单位与能量；保留参考带宽约定供回归。

<a id="s5"></a>

### S5　解耦固定自由度与整机模块组合

**优先级：按新增物理需求实施；性质：结构扩展；状态：待选择。**

固定三叶片、每片三个模态适合当前算例。增加塔架、传动链、控制器或平台后，应先设计模块的输入、输出和历史状态，重新定义耦合系统；只把数组改长不能得到正确求解器。

源码：[include/turbine/rotor.hpp，第 5–6 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/include/turbine/rotor.hpp#L5-L6)。

```cpp
namespace turbine {
// [审查] S5：转子状态固定三片叶片；ModalState 又固定三个模态。应分离叶片数与模态/自由度布局。
using RotorState = std::array<ModalState, 3>;
```

源码：[src/turbine/io/case_input.cpp，第 253–265 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/io/case_input.cpp#L253-L265)。

```cpp
    for (auto key : {"FlapDOF1", "FlapDOF2", "EdgeDOF"})
        require(structure.flag(key), std::string("Expected active blade DOF: ") + key);
    // [审查] S5：这里明确拒绝塔架、平台和传动链等自由度，属于当前支持范围。
    // [审查] 不能只删除 require：新增共享自由度后，三个独立 3×3 结构系统的假设可能失效。
    for (auto key :
         {"PitchDOF", "TeetDOF", "DrTrDOF", "GenDOF", "YawDOF", "TwFADOF1", "TwFADOF2", "TwSSDOF1",
          "TwSSDOF2", "PtfmSgDOF", "PtfmSwDOF", "PtfmHvDOF", "PtfmRDOF", "PtfmPDOF", "PtfmYDOF", "Furling"})
        require(!structure.flag(key), std::string("Unsupported active DOF: ") + key);
    // [审查] S5：尾流和 UA 模型组合也固定。建议集中声明模块能力并校验兼容组合。
    require(aero.integer("Wake_Mod") == 1 && aero.integer("BEM_Mod") == 1 && aero.integer("DBEMT_Mod") == 0,
            "Unsupported wake model");
    require(aero.integer("UA_Mod") == 3 && aero.flag("FLookup") && aero.integer("AFTabMod") == 1,
            "Unsupported unsteady airfoil model");
    require(aero.integer("TwrPotent") == 0 && aero.integer("TwrShadow") == 0 && !aero.flag("TwrAero") &&
                !aero.flag("NacelleDrag") && !aero.flag("TFinAero"),
            "Unsupported tower, nacelle or tail aerodynamics");
```

后续验证重点：逐模块验证，再验证耦合；当前三叶片九模态算例仍应作为独立回归配置。


## 真实工程需求与模型验证

<a id="e1"></a>

### E1　增加真实入流、变速变桨和降噪运行模式

**优先级：高（实机设计）；性质：新增工程能力；状态：待选择。**

需要按用途增加三维湍流、风向随高度变化、非定常入流，以及转速、桨距、偏航和控制策略。可参考 OpenFAST InflowWind/ServoDyn 的数据组织与模型，C++ 运行时仍可独立。[OpenFAST 模块目录](https://openfast.readthedocs.io/en/dev/source/user/index.html)

源码：[src/turbine/io/case_input.cpp，第 196–207 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/io/case_input.cpp#L196-L207)。

```cpp
SteadyWind::SteadyWind(const InputFile &f)
    : speed(f.number("HWindSpeed")), reference_height(f.number("RefHt")), exponent(f.number("PLExp")),
      propagation(f.number("PropagationDir") * deg), upflow(f.number("VFlowAng") * deg) {
    // [审查] E1：只接受 WindType=1；at 仅接收位置，没有时间或三维湍流场接口。
    // [审查] 扩展方向：独立 WindField 接口，支持随时间变化的三分量风速及所需空间插值。
    require(f.integer("WindType") == 1, "Only steady InflowWind WindType=1 is supported");
    require(reference_height > 0 && speed >= 0, "Invalid steady wind input");
}
Vec3 SteadyWind::at(const Vec3 &p) const {
    require(p[2] > 0 || exponent == 0, "Power law wind requested below ground");
    double v = exponent == 0 ? speed : speed * std::pow(p[2] / reference_height, exponent);
    return {v * std::cos(upflow) * std::cos(propagation), -v * std::cos(upflow) * std::sin(propagation),
            v * std::sin(upflow)};
}
```

源码：[src/turbine/structure/blade_dynamics.cpp，第 142–150 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/structure/blade_dynamics.cpp#L142-L150)。

```cpp
Matrix3 BladeStructure::blade_basis(double time, std::size_t blade) const {
    const Vec3 c2{-std::sin(tilt_) * std::cos(yaw_), -std::sin(tilt_) * std::sin(yaw_), std::cos(tilt_)},
        c3{std::sin(yaw_), -std::cos(yaw_), 0};
    // [审查] E1：方位角采用 omega*time，对应常转速。变转速需要积分方位角，并一致更新惯性项。
    const double a = initial_azimuth + omega * time + 2 * pi * blade / 3;
    const Vec3 g2 = std::cos(a) * c2 + std::sin(a) * c3, g3 = -std::sin(a) * c2 + std::cos(a) * c3;
    const Vec3 i1 = std::cos(cone[blade]) * shaft - std::sin(cone[blade]) * g3,
               i3 = std::sin(cone[blade]) * shaft + std::cos(cone[blade]) * g3;
    // [审查] E1：pitch 是当前固定输入；控制器需按时间更新桨距和转速等状态，不能只改这一个公式。
    return {{std::cos(pitch[blade]) * i1 - std::sin(pitch[blade]) * g2,
             std::sin(pitch[blade]) * i1 + std::cos(pitch[blade]) * g2, i3}};
```

后续验证重点：稳态退化一致性、风场插值、控制曲线、方位角积分；分风速与降噪模式的声学结果。

<a id="e2"></a>

### E2　建立独立的室外声传播模块

**优先级：高（场址/居民点评价）；性质：新增工程能力；状态：待选择。**

现有距离与指向性计算需要扩展到空气吸收、地面、地形和气象折射。ISO 9613-2:2024 可作为特定适用条件下的工程方法；复杂折射研究可评估射线或抛物方程方法。应避免在已有距离衰减之上再次重复施加几何衰减。[ISO 官方范围](https://www.iso.org/standard/74047.html?browse=tc)

源码：[src/acoustics/workspace.cpp，第 44–49 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/workspace.cpp#L44-L49)。

```cpp
        for (std::size_t n = 0; n < nodes.size(); ++n) {
            const auto &node = nodes[n];
            // [审查] E2：这里用当前节点与观察点计算几何，随后直接生成受声点频谱；没有场址/气象传播输入。
            // [审查] 建议定义声源参考量与传播传递量的边界，再加入传播模型，避免重复计算距离和指向性。
            const auto geometry = observe(observers[o], node.aero_center, node.global_to_local,
                                          node.section.chord, node.airfoil_reference);
            detail::emit_section(work.parameters, work.sources[n], geometry.first, geometry.second,
                                 work.weighting, output[n]);
```

源码：[src/acoustics/tno.cpp，第 111–122 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/tno.cpp#L111-L122)。

```cpp
void detail::emit_tno(const TnoSource &s, const Geometry &g, Spectrum &pressure, Spectrum &suction) {
    const double directivity = directh_te(s.mach, g.theta, g.phi);
    for (int side = 0; side < 2; ++side) {
        auto &out = side == 0 ? suction : pressure;
        out.resize(s.bandwidth.size());
        for (std::size_t i = 0; i < out.size(); ++i) {
            const double value = s.active[side]
                                     // [审查] E2：此处已有 1/r² 与指向性因子；新增传播层必须明确哪些衰减已经包含。
                                     // [审查] 本片段是现有功能的连接位置，不代表项目已实现地面、空气吸收或折射。
                                     ? 10 * std::log10(s.span / (4 * pi * g.distance * g.distance) *
                                                       s.integral[side][i] * directivity / (2e-5 * 2e-5)) +
                                           10 * std::log10(s.bandwidth[i])
                                     : 0.;
            out[i] = std::max(value, -100.);
```

后续验证重点：自由场距离规律、已知地面/吸收算例、传播模块独立验证，再与多距离实测比较。

<a id="e3"></a>

### E3　完善真实翼型、转捩和叶片表面状态输入

**优先级：高（实机设计）；性质：模型输入与适用性；状态：待选择。**

粗糙度、侵蚀、污染、转捩及分离状态需要合适的气动/边界层数据支撑。现有 Trip 开关与边界层表可作为入口，但不等于已经模拟真实表面劣化。表格应带数据来源、有效攻角/Re 区间和不确定度。[OpenFAST 边界层输入](https://openfast.readthedocs.io/en/main/source/user/aerodyn-aeroacoustics/App-usage.html)

源码：[include/aeroacoustics.hpp，第 22–27 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/include/aeroacoustics.hpp#L22-L27)。

```cpp
    // [审查] E3：x_blmethod/itrip 选择模型和经验转捩类别，并没有侵蚀高度、污染分布等真实状态参数。
    int x_blmethod = 1, itrip = 1, timod = 1, tbltemod = 1, lammod = 0, tipmod = 0, bluntmod = 0;
    bool round = true, aweighting = false;
};
struct BoundaryLayer {
    // [审查] E3：BoundaryLayer 提供尾缘特征量；更复杂壁压谱可能需要额外的压力梯度、动量厚度等输入。
    std::array<double, 2> dstar{}, d99{}, cf{}, edge_velocity_ratio{{1., 1.}};
};
```

源码：[src/acoustics/driver.cpp，第 88–92 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/driver.cpp#L88-L92)。

```cpp
    for (int k = 0; k < 8; ++k)
        v[k] = (1 - s) * ((1 - t) * values[c * n + a][k] + t * values[c * n + b][k]) +
               s * ((1 - t) * values[d * n + a][k] + t * values[d * n + b][k]);
    // [审查] E3：表格厚度乘弦长后成为有量纲量。扩充表格时要保留量纲、两侧顺序与质量检查。
    return {{{v[2] * chord, v[3] * chord}}, {{v[4] * chord, v[5] * chord}}, {{v[6], v[7]}}, {{v[0], v[1]}}};
}
```

后续验证重点：针对不同表面/转捩状态的数据来源与覆盖范围；结合翼型风洞或实机测量，评估输入敏感性。

<a id="e4"></a>

### E4　增加候选尾缘模型和锯齿等降噪装置

**优先级：按研究目标实施；性质：新增声源模型；状态：待选择。**

候选方向包括 Amiet 尾缘散射配合逆压梯度壁压谱，以及锯齿尾缘模型。需给出额外输入与适用范围，不能把“换了模型”视为自动提高精度。[壁压谱研究](https://arxiv.org/abs/2501.08134)、[锯齿尾缘实测验证](https://repository.tudelft.nl/record/uuid:15b5fff7-8258-42e8-8155-32d414f6e961)

源码：[src/acoustics/spectrum.cpp，第 78–89 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/spectrum.cpp#L78-L89)。

```cpp
    // [审查] E4：新增尾缘模型可在准备阶段接入，通过 S2 的模型选择与现有 BPM/TNO 互斥。
    if (p.tbltemod)
        prepare_trailing_edge(p, s, bl, out.trailing_edge);
    if (p.tbltemod == 2) {
        // Keep the reference section driver convention; the direct TNO kernel
        // still accepts caller-supplied edge velocity ratios.
        auto tno_section = s;
        tno_section.bl.edge_velocity_ratio = {1., 1.};
        prepare_tno(p, tno_section, work, out.tno);
    }
    // [审查] E4：当前 bluntmod 是尾缘钝度涡脱落模型，厚度/角度参数不能直接代替锯齿几何和散射模型。
    // [审查] 应显式增加锯齿幅度、波长等所需参数，并验证装置前后气动与噪声变化。
    if (p.bluntmod) {
        if (s.te_thickness <= 0 || s.te_angle < 0 || s.te_angle > 14)
            throw std::invalid_argument("Invalid bluntness geometry");
```

后续验证重点：与对应翼型试验、装置前后整机数据对照；检查零装置极限、谱形及机制重复计数。

<a id="e5"></a>

### E5　时间统计、幅度调制、运动声源和音调

**优先级：高（声音特征评价）；性质：新增工程能力；状态：待选择。**

当前频谱快照可体现低频声级变化，但没有完整的音频、窄带音调或延迟时间传播。20 s 在 10.04 rpm 下仅约 3.35 转，统计稳定性需另行检查。现有马赫数指向性修正不能当成完整的传播时延和频率映射。[受声点测量范围](https://webstore.iec.ch/en/publication/62414)

源码：[src/acoustics/driver.cpp，第 198–203 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/driver.cpp#L198-L203)。

```cpp
const Snapshot *AcousticDriver::step_view(double time, const std::vector<std::vector<Node>> &blades) {
    require(std::isfinite(time) && time > last_time_, "Times must be finite and increasing");
    require(blades.size() == blades_, "Blade count mismatch");
    // [审查] E5：按 DT_AA 采样声级快照；这不是声压信号的音频采样。
    // [审查] 建议明确源时间和接收时间，按需要增加时延、AM 分析窗口、稳态剔除和更长统计时段。
    const double phase = time + 1e-10 - dt_ * std::floor((time + 1e-10) / dt_);
    const bool sampling = time >= start_ && phase < 1e-6;
    std::size_t selected = 0;
```

源码：[src/acoustics/directivity.cpp，第 10–15 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/directivity.cpp#L10-L15)。

```cpp
double directh_te(double mach, double theta, double phi) {
    const double angle = theta * directivity_degrees, azimuth = phi * directivity_degrees;
    // [审查] E5：已有对流马赫数和方向修正。若增加运动声源频率映射，应核对公式避免重复修正。
    const double convection_mach = .8 * mach;
    return 2. * std::pow(std::sin(angle / 2.), 2) * std::pow(std::sin(azimuth), 2) /
           ((1. + mach * std::cos(angle)) * std::pow(1. + (mach - convection_mach) * std::cos(angle), 2));
}
```

后续验证重点：时间步与统计窗口收敛；已知调制信号、运动声源极限和窄带/频带转换；音调模型单独验证。

<a id="e6"></a>

### E6　机械噪声、塔架效应、多机尾流和风场噪声

**优先级：按机型/场址需求实施；性质：新增工程能力；状态：待选择。**

叶片宽带气动噪声之外，还需按实机配置考虑机械/冷却噪声、塔架相关非定常作用及多机尾流。机械音调需要独立声源数据或模型。FAST.Farm 可供多机尾流模型参考，但单机复制后简单求和不能替代尾流计算。[FAST.Farm 文档](https://openfast.readthedocs.io/en/dev/source/user/fast.farm/index.html)

源码：[src/turbine/io/case_input.cpp，第 239–241 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/io/case_input.cpp#L239-L241)。

```cpp
    // [审查] E6：当前仅支持单转子；风场层应管理各机组状态、位置、入流和共同受声点。
    require(primary.integer("NRotors") == 1 && !primary.flag("MirrorRotor"),
            "Only one normal rotor is supported");
    require(structure.integer("NumBl") == 3, "Only three blades are supported");
```

源码：[src/turbine/io/case_input.cpp，第 263–265 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/turbine/io/case_input.cpp#L263-L265)。

```cpp
    // [审查] E6：塔影、塔架和机舱气动明确禁用。开放后还需判断哪些非定常效应进入噪声模型。
    require(aero.integer("TwrPotent") == 0 && aero.integer("TwrShadow") == 0 && !aero.flag("TwrAero") &&
                !aero.flag("NacelleDrag") && !aero.flag("TFinAero"),
            "Unsupported tower, nacelle or tail aerodynamics");
```

源码：[src/apps/turbine_main.cpp，第 137–146 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L137-L146)。

```cpp
                    for (std::size_t n = 0; n < (*snapshot)[o].size(); ++n) {
                        double node_power = 0;
                        // [审查] E6：当前只汇总七类叶片声源。未来机械/冷却声源需有独立位置、频谱和指向性。
                        // [审查] 宽带非相干声源可按声能叠加；相干或确定性音调需明确相位/相关性假设。
                        for (std::size_t m = 0; m < 7; ++m)
                            for (std::size_t f = 0; f < nf; ++f) {
                                const double db = (*snapshot)[o][n][m][f],
                                             power = std::isfinite(db) ? std::pow(10., db / 10) : 0;
                                total[o] += power;
                                spectra[o * nf + f] += power;
                                mechanisms[(o * nf + f) * 7 + m] += power;
                                node_power += power;
```

后续验证重点：单机退化、多机布局和尾流场；声源叠加假设、机械声源数据以及风场实测。

<a id="e7"></a>

### E7　提供与工程测量对应的统计输出

**优先级：高（工程交付）；性质：新增后处理能力；状态：待选择。**

增加表观声功率、按风速分箱、LAeq、AM/音调指标和受声点地图，并记录平均时段、计权、背景处理与测点条件。受声点声压不能不经声场和测量条件换算就命名为声功率。IEC 61400-11 面向发射表征，IEC TS 61400-11-2:2024 面向受声点，二者用途不同。[IEC 61400-11](https://webstore.iec.ch/en/publication/5428)、[IEC TS 61400-11-2](https://webstore.iec.ch/en/publication/62414)

源码：[src/apps/turbine_main.cpp，第 141–156 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/apps/turbine_main.cpp#L141-L156)。

```cpp
                                const double db = (*snapshot)[o][n][m][f],
                                             power = std::isfinite(db) ? std::pow(10., db / 10) : 0;
                                // [审查] E7：此处把同一时刻各节点/频率的声能相加，不是对时间做等效平均。
                                total[o] += power;
                                spectra[o * nf + f] += power;
                                mechanisms[(o * nf + f) * 7 + m] += power;
                                node_power += power;
                            }
                        const auto b = n / (c.stations.size() - first),
                                   j = n % (c.stations.size() - first) + first;
                        nodal[(b * c.stations.size() + j) * no + o] = node_power;
                    }
                for (int k = 0; k < output_count; ++k) {
                    // [审查] E7：当前逐快照输出。LAeq 需对指定时间窗中的 A 计权均方声压做时间平均，再取对数。
                    // [审查] 建议独立 Statistics/EngineeringMetrics 模块，并在元数据中保存量纲、频带、计权和统计口径。
                    outputs[k] << solver.time;
                    for (double p : values[k])
                        outputs[k] << '\t' << decibels(p);
                    outputs[k] << '\n';
```

后续验证重点：用解析信号核对能量平均、计权和分箱；对照具体测量流程，不能仅因字段同名就宣称标准符合性。

<a id="e8"></a>

### E8　建立独立实测验证和不确定性评估

**优先级：高；性质：验证体系扩展；状态：待选择。**

当前 Fortran 对照证明实现一致性，不直接证明实机预测精度。需增加多风速、多方向和叶片状态的测量数据，分别检验声源与传播；处理背景噪声、气象和运行状态，并区分校准与独立验证数据。[风机传播实测与验证研究](https://wes.copernicus.org/articles/8/639/2023/wes-8-639-2023.html)

源码：[tests/run_standalone.py，第 15–29 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/tests/run_standalone.py#L15-L29)。

```python
# [审查] E8：参考目录保存的是原 Fortran 结果，属于代码间验证。
baseline=ROOT/'reference/results'/CASE
case=ROOT/'examples'/CASE
meta=json.loads((baseline/'reference.json').read_text(encoding='utf-8'))
for folder,entries in [(case,meta['inputs_sha256']),(baseline,meta['output_sha256'])]:
    for name,digest in entries.items():
        assert hashlib.sha256((folder/name).read_bytes()).hexdigest()==digest,f'Changed reference input/output: {name}'
subprocess.run([str(args.executable.resolve()),str(case/(CASE+'.fst')),str(args.output.resolve())],check=True)
runtime=json.loads((args.output/'run.json').read_text(encoding='utf-8'))
# [审查] E8：这些断言锁定一个 20 s 官方配置。应继续保留，同时建立独立测量数据验证通道。
assert runtime['solver']=='standalone C++' and runtime['steps']==3200
assert runtime['dt']==0.00625 and runtime['duration']==20 and runtime['acoustic_samples']==201
# [审查] E8：run 比较 C++ 与 Fortran 文件；不能把此误差当作对现场测量的预测误差。
report=run(baseline,args.output)
report['identical_input_files']=len(meta['inputs_sha256'])
report['runtime']=runtime
report['cpp_executable_sha256']=hashlib.sha256(args.executable.read_bytes()).hexdigest()
report['reference_openfast_commit']=meta['openfast_commit']
```

后续验证重点：新增工况、网格/步长/积分收敛、参数敏感性和误差区间；保存数据来源、测量不确定度和独立验证划分。

<a id="e9"></a>

### E9　明确 TNO 外缘速度比的参考兼容约定

**优先级：与 E3/E4 联动；性质：物理配置选择；状态：待选择。**

本项对应上一轮审查中单独指出的 TNO 细节。主路径覆盖外缘速度比是已有参考兼容约定，不是本轮新确认的移植错误。若使用真实边界层输入，应显式选择参考兼容模式或输入速度比模式，并分别验证，不能静默改变基线。

源码：[src/acoustics/spectrum.cpp，第 80–86 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/spectrum.cpp#L80-L86)。

```cpp
    if (p.tbltemod == 2) {
        // Keep the reference section driver convention; the direct TNO kernel
        // still accepts caller-supplied edge velocity ratios.
        auto tno_section = s;
        // [审查] E9：即使边界层表给出不同两侧速度比，这里仍覆盖为 {1,1}。
        // [审查] 建议用具名配置表达该约定；真实输入模式可使用表中值，但会改变 TNO 预测。
        tno_section.bl.edge_velocity_ratio = {1., 1.};
        prepare_tno(p, tno_section, work, out.tno);
    }
```

源码：[src/acoustics/tno.cpp，第 30–34 行](https://github.com/MegrezLi/Aeroacoustics_cpp/blob/3c24dedfc4282fff88d0aa533480e7dd118e98de/src/acoustics/tno.cpp#L30-L34)。

```cpp
    const int side = suction ? 0 : 1;
    const double cf = bl.cf[side], delta = bl.d99[side];
    // [审查] E9：底层积分本身支持使用传入速度比计算外缘速度，因此差别发生在上层准备阶段。
    // [审查] 验证时需要同时覆盖标量接口与整机/工作区接口，防止两条路径的配置含义不一致。
    const double uo = mach * p.spdsound * std::abs(bl.edge_velocity_ratio[side]);
    if (cf <= 0 || delta <= 0 || uo <= 0)
        throw std::invalid_argument("TNO needs positive Cf, delta and edge speed");
```

后续验证重点：速度比等于 1 时与基线一致；非 1 时验证两侧参数、速度符号约定、标量与工作区的一致性。


## 按用途选择实施范围

| 目标 | 建议先选 | 后续再评估 |
| --- | --- | --- |
| 完善独立 C++ 程序与库接口 | R1–R3、S1–S4、P1–P3 | P4–P7 按性能分析结果选择；S5 随新物理模块推进 |
| 叶片设计与降噪研究 | E1、E3、E4、E8 | E9 明确边界层数据约定；需要声音特征时加入 E5 |
| 场址或居民点噪声评价 | E1、E2、E5、E7、E8 | 整个风场加入 E6；传播前先完成 S4 的声学量定义 |

## 与 OpenFAST 的关系

OpenFAST 可作为风场、气动、结构和控制模块的参考；其 AeroAcoustics 提供频域声源模型与简化指向性，本身不等于完整的环境噪声传播及评价工具。[OpenFAST 声学模块说明](https://openfast.readthedocs.io/en/main/source/user/aerodyn-aeroacoustics/index.html)

扩展时可借鉴其模块输入、输出和历史状态分离方式，并继续用固定版本 Fortran 验证所移植的功能。引入气象传播、降噪装置或测量评价等新能力时，需要独立的理论与实测验证；不要求把整个 OpenFAST 全部移植，也不要求 C++ 运行时调用 OpenFAST。
