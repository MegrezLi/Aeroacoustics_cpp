add_executable(auxiliary_probe auxiliary_probe.cpp)
add_executable(mechanism_probe mechanism_probe.cpp)
target_link_libraries(mechanism_probe PRIVATE aeroacoustics)
target_link_libraries(auxiliary_probe PRIVATE aeroacoustics)
add_executable(acoustic_workspace_probe acoustic_workspace_probe.cpp)
target_link_libraries(acoustic_workspace_probe PRIVATE aeroacoustics)
if(AEROACOUSTICS_BUILD_TURBINE)
  add_executable(simulation_api_probe simulation_api_probe.cpp)
  target_link_libraries(simulation_api_probe PRIVATE turbine_modules)
  add_executable(turbine_workspace_probe turbine_workspace_probe.cpp)
  target_link_libraries(turbine_workspace_probe PRIVATE turbine_modules)
  add_executable(reliability_probe reliability_probe.cpp)
  target_link_libraries(reliability_probe PRIVATE turbine_modules)
  foreach(probe module structure mesh solver bem)
    add_executable(turbine_${probe}_probe turbine_${probe}_probe.cpp)
    target_link_libraries(turbine_${probe}_probe PRIVATE turbine_modules)
  endforeach()
endif()
