if(AEROACOUSTICS_BUILD_EXAMPLES)
  add_executable(aeroacoustics_example section_main.cpp)
  target_link_libraries(aeroacoustics_example PRIVATE aeroacoustics)
endif()
if(AEROACOUSTICS_BUILD_TURBINE)
  add_executable(aeroacoustics_farm farm_main.cpp)
  target_link_libraries(aeroacoustics_farm PRIVATE turbine_modules)
  add_executable(aeroacoustics_validate validation_main.cpp)
  target_link_libraries(aeroacoustics_validate PRIVATE turbine_modules)
  add_executable(aeroacoustics_batch batch_main.cpp)
  target_link_libraries(aeroacoustics_batch PRIVATE turbine_modules)
  add_executable(aeroacoustics_turbine turbine_main.cpp)
  target_link_libraries(aeroacoustics_turbine PRIVATE turbine_modules)
endif()
